# Copy this file to .env and adjust before running docker-compose
# cp .env.example .env

# Database
POSTGRES_PASSWORD=change_me_in_production
FMS_DB=fms

# Set to true on very first boot to load sample Kenya data
FMS_SEED=false

# Port Odoo listens on (host-side)
ODOO_PORT=8069
